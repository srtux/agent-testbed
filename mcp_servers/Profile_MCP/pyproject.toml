[project]
name = "profile-mcp"
version = "0.1.0"
description = "Profile_MCP service"
dependencies = [
    "agent_testbed",
]

[tool.uv]
package = false

[tool.uv.sources]
agent_testbed = { workspace = true }
