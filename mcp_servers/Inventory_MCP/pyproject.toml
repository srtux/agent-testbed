[project]
name = "inventory-mcp"
version = "0.1.0"
description = "Inventory_MCP service"
dependencies = [
    "agent_testbed",
]

[tool.uv]
package = false

[tool.uv.sources]
agent_testbed = { workspace = true }
